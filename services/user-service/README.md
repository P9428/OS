# User Service

This service provides user registration and login APIs using FastAPI.

### Development

Run locally with:

```bash
uvicorn app.main:app --reload
```

### Docker

Build the image:

```bash
docker build -t user-service .
```
